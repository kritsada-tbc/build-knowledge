<%
  //กันพัง: ถ้า server.js ส่งมาไม่ครบ จะมีค่าเริ่มต้นให้
  const SITE = (typeof site !== "undefined" && site) ? site : {};
  const page = (typeof pageTitle !== "undefined" && pageTitle) ? pageTitle : (SITE.site_name || "Thai Build Center");

  const ARTICLES  = (typeof articles !== "undefined" && Array.isArray(articles)) ? articles : [];
  const MATERIALS = (typeof materials !== "undefined" && Array.isArray(materials)) ? materials : [];
  const TOOLS     = (typeof tools !== "undefined" && Array.isArray(tools)) ? tools : [];
  const DEALERS   = (typeof dealers !== "undefined" && Array.isArray(dealers)) ? dealers : [];

  const heroTitle = SITE.hero_title || "แหล่งบทความ/ความรู้งานก่อสร้าง\nเพื่อส่งเสริมอุตสาหกรรมก่อสร้าง";
  const heroSubtitle = SITE.hero_subtitle || "รวมคำอธิบายข้อมูลวัสดุ สำหรับการใช้งาน ";

  const heroImage = SITE.hero_image_url || ""; // ถ้ามีใน /admin/ui
  const logoText = SITE.logo_text || "B";      // ถ้ามีใน /admin/ui

%>

<!doctype html>
<html lang="th">
<head>
  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-VV84RQW49P"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    gtag('config', 'G-VV84RQW49P');
  </script>

  <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7963442966946281"
     crossorigin="anonymous"></script>
     
  <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@300;400;500;600&display=swap" rel="stylesheet">
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/public/theme.css">
  <style>

    
  



  :root{
  /* ===== colors from admin (ของเดิม) ===== */
  --bg: <%= SITE.theme_bg || "#ffffff" %>;
  --card: <%= SITE.theme_card || "#f7fafc" %>;
  --text: <%= SITE.theme_text || "#0f172a" %>;
  --muted: <%= SITE.theme_muted || "#475569" %>;
  --line: <%= SITE.theme_line || "#dbe3ef" %>;
  --accent: <%= SITE.theme_accent || "#1d4ed8" %>;
  --accent2: <%= SITE.theme_accent2 || "#0b2a6f" %>;

  /* ===== semantic colors (เพิ่มใหม่) ===== */
  --bg-main: var(--bg);          /* พื้นหลังหลัก */
  --bg-soft: #f1f5ff;            /* พื้นแบ่งหมวด (คงที่ได้) */

  --heading: var(--accent2);     /* หัวข้อใหญ่ */
  --link: var(--accent);         /* ลิงก์ / ปุ่ม */
  --border-strong: var(--accent);/* เส้นแบ่งสำคัญ */
  --border-soft: var(--line);    /* เส้นทั่วไป */
  }

  /* ===== Tool Card ===== */
.tool-card{
  background: var(--card);
  border-radius: var(--radius);
  box-shadow: var(--shadow);

  /* ขอบน้ำเงินเข้มกว่า */
  border: 2px solid var(--accent2);
}


/* header */
.tool-header h3{
  margin: 0;
  color: var(--heading);
}

.tool-header p{
  margin: 4px 0 0;
  color: var(--muted);
  font-size: 14px;
}

/* form */
.tool-form{
  margin-top: var(--space-md);
  display: grid;
  gap: var(--space-sm);
}

.tool-form label{
  font-size: 14px;
  color: var(--text);
}

.tool-form input,
.tool-form select{
  padding: 10px 12px;
  border-radius: 10px;
  border: 1px solid var(--border-soft);
}

/* result */
.tool-result{
  margin-top: var(--space-md);
  padding-top: var(--space-md);
  border-top: 1px dashed var(--border-soft);
}

.tool-result b{
  color: var(--heading);
}

/* note */
.tool-note{
  margin-top: var(--space-sm);
  font-size: 13px;
  color: var(--muted);
}

  .site-footer{
  margin-top: var(--space-xl);
  padding: var(--space-lg) 0;
  border-top: 1px solid var(--border-soft);
  background: var(--bg-main);
}

.footer-admin{
  text-align: right;
}

.footer-admin a{
  font-size: 12px;
  color: var(--muted);
  text-decoration: none;
}

.footer-admin a:hover{
  color: var(--accent);
  text-decoration: underline;
}


  </style>
  <title><%= page %></title>
  <meta name="description" content="<%= SITE.meta_description || 'งานก่อสร้าง เพื่อการพัฒนาเมือง' %>" />

  <style>
    :root{
     --bg: <%= (SITE.theme_bg || "#ffffff") %>;
     --card: <%= (SITE.theme_card || "#f7fafc") %>;
     --text: <%= (SITE.theme_text || "#0f172a") %>;
     --muted: <%= (SITE.theme_muted || "#475569") %>;
     --line: <%= (SITE.theme_line || "#e2e8f0") %>;
     --accent: <%= (SITE.theme_accent || "#1d4ed8") %>;   /* น้ำเงินสดแบบทางการ */
     --accent2: <%= (SITE.theme_accent2 || "#0f172a") %>;
     --shadow: 0 10px 22px rgba(2,6,23,.08);
     --radius: 18px;
     --max: 1140px;
    }

    *{ box-sizing:border-box; }
    html,body{ height:100%; }
    body{
      margin:0;
      font-family: "Sarabun", "Noto Sans Thai", system-ui, sans-serif;
      color: var(--text);     /* สีเดิมของเว็บ */
      background: var(--bg);
      line-height: 1.8;       /* ทำให้อ่านเหมือนในภาพ */
    }
    p{
      font-size: 17px;
      margin-bottom: 1.4em;
      }



    a{ color:inherit; text-decoration:none; }
    .container{
      max-width: 1100px;
      margin: 0 auto;
    }
    @media (max-width: 768px){
      .container{
        padding-left: var(--space-md);
        padding-right: var(--space-md);
  }
}



    /* Top bar */
    .topbar{
      position: sticky; top:0; z-index:50;
      backdrop-filter: blur(12px);
      background: rgba(255,255,255,.92);
      border-bottom: 1px solid var(--line);
    }

    .nav{
      display:flex; align-items:center; justify-content:space-between;
      gap:14px; padding:14px 0;
    }
    .brand{
      display:flex; align-items:center; gap:10px;
      font-weight:800; letter-spacing:.2px;
      white-space:nowrap;
    }
    .logo{
      width:36px; height:36px; border-radius:12px;
      background: linear-gradient(135deg, var(--accent), var(--accent2));
      box-shadow: 0 10px 22px rgba(74,163,255,.18);
      display:grid; place-items:center;
      font-weight:900;
      flex:0 0 auto;
    }
    .navlinks{
      display:flex; gap:14px; align-items:center;
      color:var(--muted);
      overflow:auto;
      scrollbar-width:none;
    }
    .navlinks::-webkit-scrollbar{ display:none; }
    .navlinks a{
      padding:8px 10px; border-radius:10px;
      border:1px solid transparent;
    }
    .navlinks a:hover{
      color:var(--text);
      border-color: var(--line);
      background: rgba(255,255,255,.03);
    }

    .actions{ display:flex; gap:10px; align-items:center; }
    .btn{
      display:inline-flex; align-items:center; gap:8px;
      padding:10px 12px;
      border:1px solid var(--line);
      border-radius:12px;
      background: rgba(255,255,255,.03);
      color:var(--text);
      cursor:pointer;
      transition: transform .06s ease, background .15s ease, border-color .15s ease;
      white-space:nowrap;
    }
    .btn:hover{ background: rgba(255,255,255,.06); border-color: rgba(255,255,255,.14); }
    .btn:active{ transform: translateY(1px); }
    .btn.primary{
      border-color: var(--accent);
      background: linear-gradient(
        135deg,
        var(--accent),
        var(--accent2)
  );
    color: #ffffff;
}

    .btn.primary:hover{ border-color: rgba(74,163,255,.55); }

    /* Search */
    .search{
      flex:1;
      min-width: 240px;
      max-width: 520px;
      display:flex; align-items:center; gap:10px;
      padding:10px 12px;
      border-radius:14px;
      border:1px solid var(--line);
      background: rgba(255,255,255,.03);
    }
    .search input{
      width:100%;
      background: transparent;
      border:0;
      outline:none;
      color: var(--text);
      font-size: 14px;
    }
    .search .hint{ color: var(--muted); font-size:12px; white-space:nowrap; }

    /* Hero */
    .hero{ padding: 34px 0 14px; }
    .hero-grid{
      display:grid;
      grid-template-columns: 1.2fr .8fr;
      gap:16px;
      align-items:stretch;
    }
    .hero-card{
      border:1px solid var(--line);
      border-radius: var(--radius);
      background: rgba(255,255,255,.03);
      box-shadow: var(--shadow);
      overflow:hidden;
      position:relative;
    }

    .hero-card::before{ display:none; }

    .hero-content{ position:relative; padding: 22px 22px 18px; }
    h1{
      margin:0 0 10px;
      font-size: clamp(26px, 3vw, 40px);
      line-height:1.15;
      letter-spacing:.2px;
      white-space: pre-line;
    }
    .subtitle{
      color: var(--muted);
      margin: 0 0 14px;
      font-size: 15px;
      max-width: 60ch;
    }
    .hero-cta{ display:flex; flex-wrap:wrap; gap:10px; margin-top: 12px; }
    .chips{ display:flex; flex-wrap:wrap; gap:8px; margin-top: 14px; }
    .chip{
      font-size:12px;
      color: var(--muted);
      padding:7px 10px;
      border:1px solid var(--line);
      border-radius:999px;
      background: rgba(255,255,255,.03);
    }

    .side-card{ padding: 18px; display:flex; flex-direction:column; gap:10px; }
    .side-title{ margin:0; font-size: 14px; color: var(--muted); }
    .quicklist{ display:flex; flex-direction:column; gap:8px; }
    .quickitem{
      padding: 6px 10px;          /* 🔽 ลด padding */
      border: 1px solid var(--line);
      border-radius: 10px;        /* 🔽 ขอบเล็กลง */
      background: var(--card);
      display:flex;
      align-items:center;         /* 🔁 ไม่ต้อง align-start */
      justify-content:space-between;
      gap:6px;                    /* 🔽 ระยะห่างด้านใน */
    }

    .quickitem .card{
      padding: 0;               /* ❌ เอา padding ใหญ่ทิ้ง */
      border: none;             /* ❌ ไม่ต้องมีกรอบ */
      box-shadow: none;         /* ❌ ไม่ต้องมีเงา */
      background: transparent;  /* ❌ ไม่ต้องมีพื้นหลัง */
    }

    .quickitem h3{
      margin: 0;
      font-size: 14px;      /* ✅ เหมาะกับ list */
      line-height: 1.4;
      color: var(--text);
    }



    .quickitem:hover{ background: rgba(255,255,255,.05); border-color: rgba(255,255,255,.14); }
    .quickitem b{ display:block; margin-bottom:2px; }
    .quickitem span{ color: var(--muted); font-size: 12px; }
    .badge{
      font-size:11px;
      padding:6px 9px;
      border-radius:999px;
      border:1px solid var(--line);
      color: var(--muted);
      background: rgba(0,0,0,.12);
      white-space:nowrap;
    }

    .hero-img{
      margin-top: 14px;
      border: 1px solid var(--line);
      border-radius: 16px;
      overflow:hidden;
      background: rgba(0,0,0,.12);
    }
    .hero-img img{
      width:100%;
      height: 180px;
      object-fit: cover;
      display:block;
    }
    .hero-img .empty{
      padding: 14px;
      color: var(--muted);
      font-size: 13px;
    }

    /* Sections */
    section{
      padding: 28px 0;
    }

    /* ===== section แบ่งหมวด ===== */
    .section-soft{
       background: var(--bg-soft);
       border-top: 1px solid var(--border-soft);
       border-bottom: 1px solid var(--border-soft);
    }

    .section-head{
      display:flex; align-items:end; justify-content:space-between; gap:12px;
      margin-bottom: 10px;
    }
    .section-head h2{
     margin:0;
     font-size: 20px;
     color: var(--heading);
     padding-left: 14px;
     border-left: 6px solid var(--border-strong);
    }

    .section-head p{ margin:0; color: var(--muted); font-size: 13px; }

    .grid{
      display:grid;
      gap: var(--space-md);
    }

    .card{
      grid-column: span 4;
      border:1px solid var(--line);
      border-radius: var(--radius);
      background: rgba(255,255,255,.03);
      padding: 14px;
      transition: transform .06s ease, background .15s ease, border-color .15s ease;
    }
    .card{
      background: var(--card);
      border-radius: var(--radius);
      box-shadow: var(--shadow);

      /* ขอบสีน้ำเงิน */
      border: 1.5px solid var(--accent);
    }


   

    /* ===== card accent by category ===== */
    .card.article::before{
      background: var(--accent);
    }

    .card.material::before{
      background: #2563eb; /* น้ำเงิน */
    }

    .card.tool::before{
      background: #0ea5e9; /* ฟ้า */
    }


    .card:hover{
      transform: translateY(-4px);
      transition: .2s ease;
    }

    .card:active{ transform: translateY(1px); }
    .card h3{
       margin-top: .5rem;
       color: var(--heading);
      }
    .card p{ margin:0; color: var(--muted); font-size: 13px; }
    .card .meta{ margin-top:10px; display:flex; gap:8px; flex-wrap:wrap; }
    .pill{
      font-size:11px;
      padding:6px 9px;
      border-radius:999px;
      border:1px solid var(--line);
      color: var(--muted);
      background: rgba(0,0,0,.10);
    }
    
    .card,
    .tool-card{
      position: relative;
    }

    


    /* Tools strip */
    .tool-row{
      display:grid;
      grid-template-columns: repeat(12, 1fr);
      gap:12px;
    }
    .tool{
      grid-column: span 6;
      padding: 16px;
      border: 1px solid var(--line);
      border-radius: var(--radius);
      background: var(--card);
    }
    .tool h3{ margin:0 0 6px; font-size: 15px; }
    .tool p{ margin:0 0 12px; color: var(--muted); font-size: 13px; }
    .tool .row{ display:flex; gap:10px; flex-wrap:wrap; align-items:center; }
    .input{
      flex: 1;
      min-width: 160px;
      padding: 10px 12px;
      border-radius: 12px;
      border:1px solid var(--line);
      background: rgba(0,0,0,.14);
      color: var(--text);
      outline:none;
    }
    .result{
      margin-top: 10px;
      color: var(--muted);
      font-size: 13px;
      border-top: 1px solid var(--line);
      padding-top: 10px;
    }

    /* Footer */
    footer{
      margin-top: 20px;
      border-top:1px solid var(--line);
      padding: 20px 0 30px;
      color: var(--muted);
      font-size: 13px;
    }
    .footer-grid{
      display:grid;
      grid-template-columns: 2fr 1fr 1fr;
      gap: 14px;
    }
    .footer-grid a{ color: var(--muted); }
    .footer-grid a:hover{ color: var(--text); }
    .small{ font-size: 12px; color: var(--muted); }

    @media (max-width: 960px){
      .hero-grid{ grid-template-columns: 1fr; }
      .search{ display:none; }
      .card{ grid-column: span 6; }
      .tool{ grid-column: span 12; }
      .footer-grid{ grid-template-columns: 1fr; }
    }
    @media (max-width: 560px){
      .card{ grid-column: span 12; }
      .navlinks{ display:none; }
    }
     /* ===============================
     Mobile UI (≤ 768px)
     ================================ */
    @media (max-width: 768px){
     /* mobile styles here */
    @media (max-width: 768px){
    section{
    padding: var(--space-lg) 0;
  }
    @media (max-width: 768px){
  body{
    font-size: 16px;
    line-height: 1.7;
  }

  h1{
    font-size: 1.7rem;
  }

  h2{
    font-size: 1.25rem;
  }

  h3{
    font-size: 1.1rem;
  }
}

}

}

@media (max-width: 768px){
  .card::before{
    height: 3px;
  }
}

@media (max-width: 768px){
  .card,
  .tool-card{
    border-width: 1.5px;
  }

  .card::before,
  .tool-card::before{
    height: 3px;
  }
}



  </style>
</head>

<body>
  <!-- Topbar -->
  <div class="topbar">
    <div class="container">
      <div class="nav">
        <a class="brand" href="/">
          <div class="logo"><%= logoText %></div>
          <div>
            <%= SITE.site_name || "Thai Build Center" %>
            <div class="small"><%= SITE.site_tagline || "ศูนย์ความรู้วัสดุ เครื่องมือช่าง" %></div>
          </div>
        </a>

        <div class="search" role="search">
          <span aria-hidden="true">🔎</span>
          <input id="searchInput" type="search" placeholder="ค้นหา เช่น ปูน, เหล็ก, " />
          <span class="hint">กด Enter</span>
        </div>

        <nav class="navlinks" aria-label="เมนูหลัก">
          <a href="#categories">ข้อมูลเนื้อหา</a>
          <a href="/dealers">ตัวแทนจำหน่าย</a>
        </nav>

        <div class="actions">
          <a class="btn" href="/contact">ติดต่อ</a>
          <a class="btn primary" href="#tools">โปรแกรมคำนวณ</a>
        </div>
      </div>
    </div>
  </div>

  <!-- Hero -->
  <header class="hero">
    <div class="container">
      <div class="hero-grid">
        <div class="hero-card">
          <div class="hero-content">
            <h1><%= heroTitle %></h1>
            <p class="subtitle"><%= heroSubtitle %></p>

            <div class="hero-cta">
              <a class="btn" href="/articles">บทความงานก่อสร้าง</a>
              <a class="btn" href="/materials">วัสดุก่อสร้าง</a>
              <a class="btn" href="/tools">อุปกรณ์เครื่องมือช่าง</a>
              <a class="btn" href="/dealers">ตัวแทนจำหน่าย/แหล่งซื้อ</a>
            </div>

            <div class="hero-img">
              <% if (heroImage) { %>
                <img src="<%= heroImage %>" alt="Hero image" />
              <% } else { %>
                <div class="empty"></div>
              <% } %>
            </div>
          </div>
        </div>

        
      </div>
    </div>
  </header>

  <main class="container">


    <!-- บทความล่าสุด -->
    <section id="latest">
  <div class="section-head">
    <h2>บทความล่าสุด</h2>
    <p>คลิ๊กชื่อหัวข้อเพื่ออ่าน</p>
  </div>

  <ul class="article-list">
    <% if (!ARTICLES || ARTICLES.length === 0) { %>
      <li class="article-empty">ยังไม่มีบทความ</li>
    <% } %>

    <% (ARTICLES || []).forEach((post, index) => { %>
      <li class="article-item">
        <a href="/article/<%= post.slug %>">
          <span class="article-title">
            <%= index + 1 %>. <%= post.title %>
          </span>
          <span class="article-date">
            <%= new Date(post.created_at).toLocaleDateString('th-TH') %>
          </span>
        </a>
      </li>
    <% }) %>
  </ul>
</section>


      <!-- Categories -->
    <section id="categories" class="section-soft">
      <div class="section-head">
        <div>
          <h2>สารบัญ</h2>
          <p>แยกตามหัวข้อ</p>
        </div>
      </div>

      <div class="grid toc-grid">
        <a class="card" href="/articles" data-keywords="บทความ งานก่อสร้าง article construction">
          <h3>งานก่อสร้าง </h3>
        </a>

        <a class="card" href="/materials" data-keywords="วัสดุก่อสร้าง material cement concrete steel">
          <h3>วัสดุก่อสร้าง</h3> 
        </a>

        <a class="card" href="/tools" data-keywords="เครื่องมือช่าง tool drill saw meter">
          <h3>อุปกรณ์เครื่องมือช่าง</h3>
        </a>

        <a class="card" href="/dealers" data-keywords="ตัวแทนจำหน่าย แหล่งซื้อ dealer store supplier">
          <h3>ค้นหาสินค้า /ตัวแทนจำหน่าย / แหล่งซื้อสินค้า</h3>
        </a>

        <a class="card" href="/contact" data-keywords="ติดต่อ โฆษณา ลงบทความ contact advertise">
          <h3>ติดต่อโฆษณา / ลงบทความ</h3>
          <p>ส่งข้อความให้ผู้ดูแลเว็บไซต์</p>
        </a>

        
      </div>
    </section>    



    <!-- Tools -->
    <section id="tools" class="section-soft">
      <div class="section-head">
        <div>
          <h2>เครื่องมือคำนวณ (Tools)</h2>
          <p>คำนวณออนไลน์</p>
        </div>
      </div>

      <div class="tool-card">
        <div class="tool-header">
          <h3>🧪 คำนวณอัตราส่วนน้ำต่อปูน (Water–Cement Ratio)</h3>
          <p>ใช้ประเมินความเหมาะสมของปริมาณน้ำต่อปูนซีเมนต์ในงานก่อสร้าง</p>
      </div>

      <div class="tool-form">
         <label>น้ำหนักปูนซีเมนต์ (กิโลกรัม)</label>
         <input type="number" id="cementKg" placeholder="เช่น 50">

         <label>ปริมาณน้ำที่ใช้ (ลิตร)</label>
         <input type="number" id="waterLiter" placeholder="เช่น 25">

         <button class="btn primary" onclick="calcWC()">
          คำนวณ W/C
      </button>
      </div>

      <div id="wcResult" class="tool-result"></div>
     </div>

     <script>
function calcWC(){
  const cement = parseFloat(document.getElementById("cementKg").value);
  const water = parseFloat(document.getElementById("waterLiter").value);

  if(!cement || !water || cement <= 0 || water <= 0){
    alert("กรุณากรอกข้อมูลให้ถูกต้อง");
    return;
  }

  const wc = water / cement;
  let quality = "";
  let advice = "";

  if(wc < 0.40){
    quality = "ค่อนข้างแห้ง (กำลังอัดสูง แต่เทยาก)";
    advice = "เหมาะกับงานโครงสร้างที่ควบคุมคุณภาพดี ใช้เครื่องสั่นช่วย";
  }else if(wc <= 0.55){
    quality = "เหมาะสม";
    advice = "เหมาะกับงานคอนกรีตทั่วไป ให้กำลังอัดและการทำงานที่ดี";
  }else{
    quality = "เหลวเกินไป (เสี่ยงกำลังอัดลด)";
    advice = "เหมาะกับงานฉาบหรือก่อ ไม่แนะนำงานโครงสร้าง";
  }

  document.getElementById("wcResult").innerHTML = `
    <p><b>ค่า Water–Cement Ratio (W/C)</b></p>
    <p>น้ำ ${water} ÷ ปูน ${cement} = <b>${wc.toFixed(2)}</b></p>

    <hr style="margin:12px 0">

    <p><b>การแปลผล</b></p>
    <p>สถานะ: <b>${quality}</b></p>

    <hr style="margin:12px 0">

    <p><b>อธิบายวิธีคำนวณ</b></p>
    <p style="font-size:14px;color:#475569;">
      สูตรที่ใช้คือ<br>
      <b>W/C = ปริมาณน้ำ (กก. หรือ ลิตร) ÷ น้ำหนักปูน (กก.)</b><br><br>
      ในน้ำ 1 ลิตร ≈ 1 กิโลกรัม<br>
      ดังนั้น ${water} ÷ ${cement} = ${wc.toFixed(2)}
    </p>

    <hr style="margin:12px 0">

    <p><b>คำแนะนำการใช้งานจริง</b></p>
    <p style="font-size:14px;color:#475569;">
      ${advice}<br>
      ควรชั่งน้ำและปูนให้แม่นยำ และหลีกเลี่ยงการเติมน้ำเพิ่มหน้างานโดยไม่คำนวณ
    </p>
  `;
}
</script>



      <div class="tool-card">
        <div class="tool-header">
          <h3>🧩 คำนวณจำนวนกระเบื้อง ออนไลน์</h3>
          <p>ประเมินจำนวนกระเบื้องที่ต้องใช้สำหรับปูพื้นหรือผนัง</p>
        </div>

        <div class="tool-form">
          <label>ความกว้างพื้นที่ (เมตร)</label>
          <input type="number" id="areaWidth" placeholder="กรอกตัวเลข">

          <label>ความยาวพื้นที่ (เมตร)</label>
          <input type="number" id="areaLength" placeholder="กรอกตัวเลข">

          <label>ขนาดกระเบื้อง (ซม.)</label>
          <select id="tileSize" onchange="toggleCustomTile()">
            <option value="60x60">60 × 60</option>
            <option value="30x30">30 × 30</option>
            <option value="30x60">30 × 60</option>
            <option value="custom">กำหนดเอง</option>
          </select>

          <div id="customTile" style="display:none;">
            <label>กว้าง (ซม.)</label>
            <input type="number" id="tileW">

            <label>ยาว (ซม.)</label>
            <input type="number" id="tileL">
          </div>

          <button class="btn primary" onclick="calcTile()">
             คำนวณ
          </button>
       </div>

       <div id="tileResult" class="tool-result"></div>

       <div class="tool-note">
           ℹ️ แนะนำเผื่อกระเบื้องแตกเสียอย่างน้อย 5–10%
       </div>
      </div>

      <script>
function toggleCustomTile(){
  const size = document.getElementById("tileSize").value;
  document.getElementById("customTile").style.display =
    size === "custom" ? "block" : "none";
}

function calcTile(){
  const w = parseFloat(document.getElementById("areaWidth").value);
  const l = parseFloat(document.getElementById("areaLength").value);
  const size = document.getElementById("tileSize").value;

  if(!w || !l){
    alert("กรุณากรอกขนาดพื้นที่ให้ครบ");
    return;
  }

  let tileW, tileL;

  if(size === "custom"){
    tileW = parseFloat(document.getElementById("tileW").value);
    tileL = parseFloat(document.getElementById("tileL").value);

    if(!tileW || !tileL){
      alert("กรุณากรอกขนาดกระเบื้อง");
      return;
    }
  }else{
    const parts = size.split("x");
    tileW = parseFloat(parts[0]);
    tileL = parseFloat(parts[1]);
  }

  const area = w * l;
  const tileArea = (tileW/100) * (tileL/100);

  const qty = Math.ceil(area / tileArea);
  const total = Math.ceil(qty * 1.10); // เผื่อ 10%

  document.getElementById("tileResult").innerHTML = `
    <p>พื้นที่ทั้งหมด: <b>${area.toFixed(2)}</b> ตร.ม.</p>
    <p>ขนาดกระเบื้อง: <b>${tileW} × ${tileL}</b> ซม.</p>
    <p>จำนวนกระเบื้องที่ใช้: <b>${qty}</b> แผ่น</p>
    <p>เผื่อแตกเสีย 10%: <b>${total}</b> แผ่น</p>
  `;
}
</script>



      <div class="tool-card">
  <div class="tool-header">
    <h3>🧱 คำนวณจำนวนอิฐ / บล็อก ออนไลน์</h3>
    <p>ประเมินปริมาณวัสดุสำหรับผนังหน้างาน</p>
  </div>

  <div class="tool-form">
    <label>ความยาวผนัง (เมตร)</label>
    <input type="number" id="wallLength" placeholder="กรอกตัวเลข">

    <label>ความสูงผนัง (เมตร)</label>
    <input type="number" id="wallHeight" placeholder="กรอกตัวเลข">

    <label>ประเภทวัสดุ</label>
    <select id="material">
      <option value="brick">อิฐมอญ</option>
      <option value="block">บล็อกคอนกรีต</option>
    </select>

    <button class="btn primary" onclick="calcBrick()">
      คำนวณ
    </button>
  </div>

  <div id="result" class="tool-result"></div>

  <div class="tool-note">
    ℹ️ ใช้เพื่อประเมินหน้างาน อาจแตกต่างตามฝีมือช่างและการสูญเสีย
  </div>
</div>
        
        <div class="tool-card">
  <div class="tool-header">
    <h3>🪣 คำนวณปูนก่อ / ปูนฉาบ ออนไลน์</h3>
    <p>คำนวณจำนวนถุงปูนที่ต้องใช้โดยประมาณ</p>
  </div>

  <div class="tool-form">
    <label>พื้นที่ผนัง (ตร.ม.)</label>
    <input type="number" id="plasterArea" placeholder="กรอกตัวเลข">

    <label>ประเภทงาน</label>
    <select id="workType">
      <option value="masonry">งานก่ออิฐ / บล็อก</option>
      <option value="plaster">งานฉาบผนัง</option>
    </select>

    <button class="btn primary" onclick="calcCement()">
      คำนวณ
    </button>
  </div>

  <div id="cementResult" class="tool-result"></div>

  <div class="tool-note">
    ℹ️ ปริมาณขึ้นกับความหนาและสภาพผนัง แนะนำซื้อเผื่อเล็กน้อย
  </div>
</div>

        <div class="grid">
  <!-- tool-card 1 -->
  <!-- tool-card 2 -->
        </div>

        <script>
        function calcCement(){
          const area = parseFloat(document.getElementById("plasterArea").value);
          const type = document.getElementById("workType").value;

        if(!area || area <= 0){
          alert("กรุณากรอกพื้นที่ผนังให้ถูกต้อง");
          return;
        }

        let rate = 0;
        let workName = "";
        let note = "";

        if(type === "masonry"){
          rate = 0.02;
          workName = "งานก่ออิฐ / บล็อก";
          note = "เหมาะสำหรับงานก่อทั่วไป ความหนาปูนปกติ  ";
        }else{
          rate = 0.15;
          workName = "งานฉาบผนัง";
          note = "คำนวณจากความหนาฉาบประมาณ 1 ซม.";
        }

        const bags = area * rate;
        const total = Math.ceil(bags * 1.10); // เผื่อ 10%

        document.getElementById("cementResult").innerHTML = `
          <p><b>${workName}</b></p>
          <p>พื้นที่ผนัง: <b>${area.toFixed(2)}</b> ตร.ม.</p>
          <p>ใช้ปูนประมาณ: <b>${total}</b> ถุง (ถุงขนาด 50 กก.)</p>
          <p style="color:#475569;font-size:14px;">
            ${note}<br>
            * เผื่อสูญเสีย 10% สำหรับหน้างานจริง
          </p>
        `;
        }
        </script>

        <script>
        function calcBrick(){
          const length = parseFloat(document.getElementById("wallLength").value);
          const height = parseFloat(document.getElementById("wallHeight").value);
          const material = document.getElementById("material").value;

          if(!length || !height){
            alert("กรุณากรอกขนาดผนังให้ครบ");
            return;
          }

          const area = length * height;

          let perSqm = 0;
          let name = "";

          if(material === "brick"){
            perSqm = 120;
            name = "อิฐมอญ";
          }else{
            perSqm = 12.5;
            name = "บล็อกคอนกรีต";
          }

          const qty = Math.ceil(area * perSqm);
          const waste = Math.ceil(qty * 0.05);
          const total = qty + waste;

          document.getElementById("result").innerHTML = `
            <p>พื้นที่ผนัง: <b>${area.toFixed(2)}</b> ตร.ม.</p>
            <p>ใช้${name}ประมาณ: <b>${qty}</b> ก้อน</p>
            <p>เผื่อแตกเสีย 5% ควรใช้จำนวน: <b>${total}</b> ก้อน</p>
            <small>* คำนวณเพื่อประเมินหน้างานสำหรับสั่งซื้อคร่าวๆเท่านั้น</small>
         `;
        }
        </script>


      </div>
    </section>

      

    <!-- Contact -->
    <section id="contact">
      <div class="section-head">
        <div>
          <h2>ติดต่อโฆษณา / ลงบทความ</h2>
          <p>ส่งข้อความหาแอดมินได้ที่หน้า Contact (เข้า inbox หลังบ้าน)</p>
        </div>
        <a class="btn primary" href="/contact">ไปที่หน้าติดต่อ</a>
      </div>

      <div class="grid">
        <div class="card" style="grid-column: span 7;">
          <h3>มีส่วนร่วมกับเรา</h3>
          <p>
            • ส่งหัวข้อที่อยากให้เขียน<br/>
            • ติดต่อโฆษณา/สปอนเซอร์<br/>
            • เสนอข้อมูลแหล่งซื้อ/ร้านค้า<br/>
            • ติชม แนะนำ เพื่อพัฒนา<br/>
          </p>
          <div class="meta"><span class="pill">ส่งเข้า Inbox</span><span class="pill">อ่านใน Admin</span></div>
        </div>

        <div class="card" style="grid-column: span 5;">
          <h3>นโยบายเว็บ</h3>
          <p>
            • Website นี้สร้างขึ้นเพื่อเผยแพร่ความรู้วัสดุก่อสร้างและอุปกรณ์<br/>
            • เนื้อหาเป็นแนวทางทั่วไป ใช้หน้างานควรพิจารณาสภาพจริง<br/>
            • Website นี้จัดสร้างขึ้นโดย KRITSADA TEAKWOOD FACTORY
          </p>
          <div class="meta"><span class="pill">About</span><span class="pill">Disclaimer</span></div>
        </div>
      </div>
    </section>

  </main>

  <footer>
    <div class="container">
      <div class="footer-grid">
        <div>
          <div style="display:flex; align-items:center; gap:10px; margin-bottom:6px;">
            <div class="logo" style="width:30px;height:30px;border-radius:10px;"><%= logoText %></div>
            <b><%= SITE.site_name || "Thai Build Center" %></b>
          </div>
          <div class="small">
            © <span id="year"></span> • THAI BUILD CENTER งานก่อสร้างเพื่อการพัฒนาเมือง
          </div>
        </div>

        <div>
          <b>หมวดหลัก</b><br/>
          <a href="/articles">บทความ</a><br/>
          <a href="/materials">วัสดุ</a><br/>
          <a href="/tools">เครื่องมือ</a><br/>
          <a href="/dealers">ตัวแทนจำหน่าย</a>
        </div>

        <div>
          <b>อื่นๆ</b><br/>
          <a href="/contact">ติดต่อแอดมิน</a><br/>
        </div>
      </div>
    </div>
  </footer>

  <script>
    document.getElementById("year").textContent = new Date().getFullYear();

    // ===== Search filter (cards + quick items that have data-keywords) =====
    const searchInput = document.getElementById("searchInput");
    const searchable = Array.from(document.querySelectorAll("[data-keywords]"));

    function applySearch(q){
      const query = (q || "").trim().toLowerCase();
      if(!query){
        searchable.forEach(el => el.style.display = "");
        return;
      }
      searchable.forEach(el => {
        const keys = (el.getAttribute("data-keywords") || "").toLowerCase();
        const title = (el.querySelector("h3")?.textContent || "").toLowerCase();
        const match = keys.includes(query) || title.includes(query);
        el.style.display = match ? "" : "none";
      });
    }
    if(searchInput){
      searchInput.addEventListener("keydown", (e) => {
        if(e.key === "Enter") applySearch(searchInput.value);
      });
    }

    // ===== Tools =====
    function toNum(v){ const n = Number(v); return Number.isFinite(n) ? n : 0; }

    window.calcConcrete = function(){
      const w = toNum(document.getElementById("c_w").value);
      const l = toNum(document.getElementById("c_l").value);
      const t = toNum(document.getElementById("c_t").value);
      const vol = w * l * t;
      document.getElementById("c_out").textContent =
        "ผลลัพธ์: " + (vol > 0 ? vol.toFixed(3) + " ลบ.ม. (m³)" : "—");
    }

    window.calcTiles = function(){
      const area = toNum(document.getElementById("t_area").value);
      const tile = toNum(document.getElementById("t_tile").value);
      const waste = toNum(document.getElementById("t_waste").value);

      if(area <= 0 || tile <= 0){
        document.getElementById("t_out").textContent = "ผลลัพธ์: —";
        return;
      }

      const base = area / tile;
      const total = Math.ceil(base * (1 + (waste/100)));
      document.getElementById("t_out").textContent =
        "ผลลัพธ์: ประมาณ " + total.toLocaleString() + " แผ่น (รวมเผื่อแตก " + waste + "%)";
    }
    window.toggleCemFields = function(){
  // ตอนนี้ทุกงานใช้ area+thickness เหมือนกัน (ไม่ต้องซ่อน)
  // เผื่ออนาคตอยากเพิ่ม field เช่น เกรดคอนกรีต/สัดส่วน ก็ขยายตรงนี้ได้
};

window.calcCementBags = function(){
  const job = document.getElementById("cem_job").value;
  const area = Number(document.getElementById("cem_area").value || 0);     // m2
  const thk_mm = Number(document.getElementById("cem_thk").value || 0);   // mm
  const bagKg = Number(document.getElementById("cem_bag").value || 50);

  const out = document.getElementById("cem_out");
  if(area <= 0 || thk_mm <= 0){
    out.textContent = "ผลลัพธ์: — (กรุณากรอกพื้นที่และความหนา)";
    return;
  }

  // ----- ตัวตั้งต้นการคำนวณ -----
  // 1) งานก่อ/ฉาบ (ปูนก่อ-ฉาบ)
  //    ใช้ค่าโดยประมาณ: ซีเมนต์ ~ 6.0 kg ต่อ 1 m2 ต่อ 1 mm (ช่วงจริง 5–7 ขึ้นกับสูตร/ทราย/ความชื้น/ฝีมือ)
  // 2) งานโครงสร้าง (คอนกรีต)
  //    คิดจากปริมาตร = area * thickness(m)
  //    และใช้สูตรมาตรฐานประมาณ: ซีเมนต์ ~ 300 kg ต่อ 1 m3 (ช่วงจริง 280–350 แล้วแต่เกรด)
  let cementKg = 0;

  if(job === "plaster"){
    const kg_per_m2_per_mm = 6.0;
    cementKg = area * thk_mm * kg_per_m2_per_mm;
  }else{
    const thickness_m = thk_mm / 1000;
    const volume_m3 = area * thickness_m;
    const kg_per_m3 = 300; // ปรับได้ถ้าคุณใช้เกรดสูงขึ้น
    cementKg = volume_m3 * kg_per_m3;
  }

  // เผื่อสูญเสีย 10%
  const waste = 0.10;
  const cementKgWithWaste = cementKg * (1 + waste);

  const bags = Math.ceil(cementKgWithWaste / bagKg);

  const label = (job === "plaster")
    ? "งานก่อ/ฉาบ"
    : "งานโครงสร้าง (คอนกรีต)";

  out.textContent =
    `ผลลัพธ์: ${label} ใช้ซีเมนต์ประมาณ ${cementKgWithWaste.toFixed(1)} กก. ≈ ${bags.toLocaleString()} ถุง (ถุงละ ${bagKg} กก., เผื่อสูญเสีย 10%)`;
};

  </script>

  <footer class="site-footer">
  <div class="container">
    <div class="footer-main">
      <p>© <%= new Date().getFullYear() %> ความรู้วัสดุก่อสร้าง | คำนวณหน้างาน | Build Knowledge</p>
    </div>

    <div class="footer-admin">
      <a href="/admin/login">Admin</a>
    </div>
  </div>
  </footer>


</body>
</html>
