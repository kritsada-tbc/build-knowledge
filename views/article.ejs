<%- include("layout", { pageTitle, body: `
  <article>
    
  <section>
  <div class="section-head">
    <div>
      <h2>บทความงานก่อสร้าง</h2>
      <p>ความรู้ เทคนิค และแนวทางใช้งานจริง</p>
    </div>
  </div>

  <div class="grid">
    <div class="card">บทความ 1</div>
    <div class="card">บทความ 2</div>
    <div class="card">บทความ 3</div>
  </div>
  </section>

  <section class="section-soft">
  <div class="section-head">
    <h2>เครื่องมือคำนวณ</h2>
    <p>คำอธิบาย</p>
  </div>

  <div class="grid">
    <div class="card">...</div>
    <div class="card">...</div>
  </div>
  
  </section>

    <h1>${post.title}</h1>
    <link rel="stylesheet" href="/public/theme.css">
    <div class="muted small">
      อัปเดต: ${new Date(post.updated_at).toLocaleDateString("th-TH")}
      • แท็ก: ${(post.tags || "").replaceAll(",", ", ")}
    </div>
    <hr style="border:0;border-top:1px solid rgba(255,255,255,.10); margin: 12px 0;" />
    <div class="content">
      ${post.content_html}
    </div>
    
    
  </article>
  
` }) %>
